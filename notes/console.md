dynamic router
-> Context menu
--> Settings + Logs

Make sure that there is a small vertical box next to the status indicator like: ❚
Make sure the color reflects the status
The connection-widget-info modules should utilize something like this
> display: grid; grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));

